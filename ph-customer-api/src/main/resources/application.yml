spring:
  application:
    name: ph-customer-api

server:
  port: 10006
  
# Define the Eureka server that handles service registration
eureka:
  instance:
    leaseRenewalIntervalInSeconds: 10
  client:
    serviceUrl:
      defaultZone: http://localhost:10001/eureka/